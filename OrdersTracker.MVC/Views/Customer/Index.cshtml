@model List<OrdersTracker.Contracts.Models.CustomerModel>

<h1>Customers</h1>

<table class="table">
    <thead>
        <tr>
            <th>Customer ID</th>
            <th>Contact Name</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var customer in Model)
        {
            <tr>
                <td>@customer.CustomerID</td>
                <td>@customer.ContactName</td>
            </tr>
        }
    </tbody>
</table>
